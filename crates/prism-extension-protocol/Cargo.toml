[package]
name = "prism-extension-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "MIT"
description = "Stable serialized protocol types for Prism extensions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

