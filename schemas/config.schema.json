{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/NathanaelRea/prism/main/schemas/config.schema.json",
  "title": "Prism Config",
  "description": "Configuration for Prism user and repository config.toml files.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "default_harness": {
      "type": "string",
      "default": "opencode",
      "description": "Global harness ID. OpenCode, Codex, Claude, and Pi have built-in definitions; other IDs must be defined in the harnesses table."
    },
    "default_base": {
      "type": "string",
      "default": "main",
      "description": "Default branch for this repository. Prism does not treat this branch as a pull request branch."
    },
    "plan_dir": {
      "type": "string",
      "default": "plans",
      "description": "Directory used for plan files."
    },
    "review_packet_dir": {
      "type": "string",
      "default": ".agent/review",
      "description": "Directory used for review packet files."
    },
    "worktree_command": {
      "type": "string",
      "default": "wt",
      "description": "Command used for worktree operations."
    },
    "opencode_port_base": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65535,
      "default": 41000,
      "description": "Base port for Prism-managed OpenCode servers."
    },
    "opencode_port_span": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "default": 1000,
      "description": "Number of ports in the deterministic OpenCode server port range."
    },
    "opencode_shutdown_owned_servers": {
      "type": "boolean",
      "default": false,
      "description": "Whether Prism should shut down OpenCode servers it started when Prism exits."
    },
    "opencode_plan_plugin": {
      "type": "boolean",
      "default": false,
      "description": "Whether Prism should install and use the OpenCode plan plugin."
    },
    "escape_key": {
      "type": "string",
      "enum": ["esc-esc", "ctrl-space"],
      "default": "esc-esc",
      "description": "Key sequence used to leave embedded terminal sessions."
    },
    "merge_method": {
      "type": "string",
      "enum": ["squash", "merge", "rebase"],
      "default": "squash",
      "description": "GitHub merge method Prism uses for pull requests."
    },
    "ui": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "icon_style": {
          "type": "string",
          "enum": ["unicode", "nerd-font"],
          "default": "unicode",
          "description": "Status icon style. nerd-font requires a Nerd Font in the terminal."
        }
      }
    },
    "checks": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "pre_pr": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Commands to run before opening a pull request."
        },
        "pre_push": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Commands to run before pushing."
        },
        "review_fix": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Commands to run after applying review fixes."
        }
      }
    },
    "auto": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "merge": {
          "type": "boolean",
          "default": false,
          "description": "Whether Auto Flow may merge pull requests."
        },
        "cleanup_after_merge": {
          "type": "boolean",
          "default": false,
          "description": "Whether Auto Flow should clean up branches/worktrees after merging."
        },
        "require_review_approval": {
          "type": "boolean",
          "default": false,
          "description": "Whether PR Stabilization treats missing review approval as a blocker independent of repository policy."
        },
        "push_initial": {
          "type": "boolean",
          "default": true,
          "description": "Whether Auto Flow may push the initial implementation commit and open a pull request automatically."
        },
        "push_repairs": {
          "type": "boolean",
          "default": false,
          "description": "Whether managed PR repair commits may be pushed automatically instead of waiting for user inspection."
        },
        "review_wait_enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether Auto Flow waits for review results."
        },
        "review_reviewer_identities": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["Copilot", "github-copilot"],
          "description": "Reviewer identities Auto Flow treats as automated review feedback."
        },
        "review_max_wait_seconds": {
          "type": "integer",
          "minimum": 0,
          "default": 300,
          "description": "Maximum time to wait for review feedback."
        },
        "review_poll_interval_seconds": {
          "type": "integer",
          "minimum": 1,
          "default": 30,
          "description": "Polling interval while waiting for review feedback."
        },
        "review_continue_on_timeout": {
          "type": "boolean",
          "default": true,
          "description": "Whether Auto Flow continues when review waiting times out."
        },
        "ci_wait_enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether Auto Flow waits for CI."
        },
        "ci_max_wait_seconds": {
          "type": "integer",
          "minimum": 0,
          "default": 1800,
          "description": "Maximum time to wait for CI."
        },
        "ci_poll_interval_seconds": {
          "type": "integer",
          "minimum": 1,
          "default": 30,
          "description": "Polling interval while waiting for CI."
        }
      }
    },
    "layout": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "sidebar_width": {
          "type": "integer",
          "minimum": 20,
          "maximum": 120,
          "description": "Preferred sidebar width in terminal columns."
        }
      }
    },
    "worktrees": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "columns": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Extra wt list JSON columns shown in the worktree list."
        }
      }
    },
    "tools": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Command overrides for external tools."
    },
    "harnesses": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "adapter": {
            "type": "string",
            "enum": ["opencode", "codex", "claude", "pi", "generic"],
            "default": "generic"
          },
          "program": {
            "type": "string",
            "description": "Built-in adapter executable or wrapper. Prism owns protocol arguments."
          },
          "arguments": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Adapter-approved arguments placed before Prism-owned protocol arguments."
          },
          "interactive_command": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" }
          },
          "interactive_prompt_transport": {
            "type": "string",
            "enum": ["argument", "temp-file"]
          },
          "headless_command": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" }
          },
          "headless_prompt_transport": {
            "type": "string",
            "enum": ["argument", "stdin", "temp-file"]
          },
          "output_format": {
            "type": "string",
            "enum": ["text"],
            "default": "text"
          },
          "environment": {
            "type": "object",
            "additionalProperties": { "type": "string" }
          }
        }
      }
    },
    "prompt_templates": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Prompt templates keyed by template name."
    }
  }
}
