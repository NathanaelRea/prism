Implement the supplied Task in the exact Worktree Session.

Do not commit, push, create a Change Request, merge, or delete the worktree. Return a typed
candidate-change Artifact with the exact resulting head and verification request.
